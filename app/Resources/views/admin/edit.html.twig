{% extends 'base.html.twig' %}

{% block main %}

    <div class="container body-content span=8 offset=2">
        <div class="well">
            <form class="form-horizontal" action="{{ path('profile_edit', {id: user.id}) }}" method="post">
                <fieldset>
                    <legend>Edit User</legend>
                            {#{% for role in allRoles %}#}
                    <div class="form-group">
                        <label for="exampleFormControlSelect1">Example select</label>
                        <select class="form-control" id="exampleFormControlSelect1">
                    {#<div class="form-group">#}
                        {#<label for="role">Select list:</label>#}
                        {#<select class="form-control" id="role">#}
                        {#<label class="col-sm-4 control-label" for="user_email">ROLE</label>#}
                        {#<div class="col-sm-4 ">#}
                            {#<input class="form-control" placeholder="ROLE" name="user[role]" required type="text" value="{{ user.roles }}">#}


                            {#<option value="{{ role.id }}">{{ role.name|join}}</option>#}
                            <option value="ROLE_ADMIN">ROLE_ADMIN</option>
                            <option value="ROLE_USER">ROLE_USER</option>
                            {#<option value="{{ role.id }}">{{ role.name}}</option>#}


                        </select>
                            {#{% endfor %}#}
                        {#</div>#}
                    </div>
                    {#<div class="form-group">#}
                        {#<label class="col-sm-4 control-label" for="user_fullName">Full Name</label>#}
                        {#<div class="col-sm-4 ">#}
                            {#<input type="text" class="form-control" placeholder="Full Name" name="user[fullName]" required value="{{ user.fullName }}">#}
                        {#</div>#}
                    {#</div>#}
                    {#<div class="form-group">#}
                        {#<label class="col-sm-4 control-label" for="user_password">Password</label>#}
                        {#<div class="col-sm-4">#}
                            {#<input type="password" class="form-control" placeholder="Password" name="user[password]" required value="{{ user.password }}">#}
                        {#</div>#}
                    {#</div>#}
                    {#<div class="form-group">#}
                    {#<label class="col-sm-4 control-label" for="user_password_second">Confirm Password</label>#}
                    {#<div class="col-sm-4">#}
                    {#<input type="password" class="form-control" id="user_password_second" placeholder="Password" name="user[password][second]" required>#}
                    {#</div>#}
                    {#</div>#}
                    <div class="form-group">
                        <div class="col-sm-4 col-sm-offset-4">
                            <a class="btn btn-default" href="{{ path('homepage') }}">Cancel</a>
                            <button type="submit" class="btn btn-primary">Edit</button>
                        </div>
                    </div>

                    {{  form_row(form._token) }}
                </fieldset>
            </form>
        </div>
    </div>

{% endblock %}

{#{% block body_id %}#}

{#{% endblock %}#}

